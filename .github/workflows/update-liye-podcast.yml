name: Update Liye podcast feed

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Refresh feed from Xiaoyuzhou
        run: python3 liye-podcast/update_feed.py --output liye-podcast/feed.xml
      - name: Commit new episodes
        run: |
          date -u +%Y-%m > liye-podcast/last-checked.txt
          git add liye-podcast/feed.xml liye-podcast/last-checked.txt
          if ! git diff --cached --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "Update Liye podcast feed"
            git push
          fi
